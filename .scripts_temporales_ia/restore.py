import urllib.request
import json

# Just reconstruct the combined file from the VCV file and the SJ script output.
# Since we already fetched SJ into partidos_sj.txt and VCV was already extracted into extract_matches output,
# Actually, I can just re-run the fetchers, or use the backup!

