# Lista de Tareas (Task List)

- [ ] 1. Modificar `script-dev/Código.js` para crear la Fase 1 (`login`) que devuelva solo credenciales y permisos.
- [ ] 2. Añadir la Fase 2 (`get_data`) en `script-dev/Código.js` para leer todos los partidos y puntos.
- [ ] 3. Integrar en `get_data` la lectura de las hojas `Plantillas`, `Resultados Oficiales` (Declaraciones) y `Votos_Destacado`.
- [ ] 4. Crear la acción `vote_destacado` en el backend para recibir y guardar los votos.
- [ ] 5. Modificar el JavaScript de `dev.html` para gestionar el login asíncrono (mostrar mensajes de "Cargando partidos...").
- [ ] 6. Implementar en `dev.html` la interfaz del "Jugador Destacado" (CSS y HTML).
- [ ] 7. Añadir la lógica de tiempos en `dev.html` (abrir votación a la hora, mostrar ganador a las 24h).
- [ ] 8. Implementar la carga visual de la foto del jugador destacado desde la carpeta `files/jugadores/`.

